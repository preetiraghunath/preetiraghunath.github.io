{
  "name": "preeti-raghunath-portfolio",
  "version": "2.0.0",
  "description": "Academic portfolio site built with Eleventy",
  "scripts": {
    "dev": "eleventy --serve --pathprefix=/v2/",
    "build": "eleventy",
    "clean": "rm -rf v2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
